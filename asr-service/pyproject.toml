[project]
name = "asr-service"
version = "0.1.0"
description = "ASR microservice using faster-whisper"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.6",
    "uvicorn[standard]>=0.34.0",
    "faster-whisper>=1.1.0",
    "python-multipart>=0.0.19",
    "numpy<2",
    "requests",
]
